{% extends 'facturas/base_factura.html.twig' %}

   {% block body %}
      <div class="row mt-5">
        <div class="col-md-3"></div>
        <div class="col-md-6 text-center">
          <div class="p-5 text-center bg-light border border-primary rounded">
          {{ form_start(form) }}
          {{ form_errors(form) }}

          <div class="py-4">
          {{ form_row(form.numerofactura) }}
          </div> 

          <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
          {{ form_end(form) }}
            </div> 
            <div class="col-md-3"></div>
          </div> 
          </div> 
        </div> 
        <div class="col-md-3"></div>
      </div> 

    {% endblock %}

    {% block javascripts %}
    {% endblock %}

